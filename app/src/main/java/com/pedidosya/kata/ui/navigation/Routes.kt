package com.pedidosya.kata.ui.navigation

/**
 * Route identifiers for [KataNavHost].
 *
 * The `summary?code={code}&pct={pct}&category={category}` route from
 * `sdd/shopping-cart/design` is wired in a later phase (Phase 7); only the cart route exists
 * here.
 */
object Routes {
    const val CART = "cart"
}
